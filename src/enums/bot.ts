export enum BotStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum BotTabsEnum {
  PLAYGROUND = "playground",
  ANALYTICS = "analytics",
}

export enum RoleEnum {
  USER = "user",
  ASSISTANT = "assistant",
  SYSTEM = "system",
  DEVELOPER = "developer",
}
