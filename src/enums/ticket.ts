export enum TicketStatus {
  OPEN = "open",
  CLOSED = "closed",
  RESOLVED = "resolved",
  IN_PROGRESS = "in_progress",
}

export enum TicketRoleEnum {
  SUPPORT = "support",
  USER = "user",
}

export enum TicketPriority {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}
